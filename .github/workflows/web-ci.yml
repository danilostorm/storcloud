name: Web CI

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - 'update.sh'
      - '.github/workflows/web-ci.yml'
  pull_request:
    paths:
      - 'frontend/**'
      - 'update.sh'
      - '.github/workflows/web-ci.yml'

jobs:
  web-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate Nginx configuration
        run: |
          docker run --rm \
            -v "$PWD/frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro" \
            nginx:1.27-alpine nginx -t
      - name: Validate required launcher files
        run: |
          for f in \
            frontend/index.html \
            frontend/catalog/index.html \
            frontend/library/index.html \
            frontend/account/index.html \
            frontend/admin/index.html \
            frontend/pc/index.html \
            frontend/achievements/index.html \
            frontend/assets/console.css \
            frontend/assets/spatial-nav.js; do
            test -f "$f"
          done
          grep -q 'healthz' frontend/index.html
          grep -q 'claim-admin' frontend/account/index.html
          grep -q 'scanAll' frontend/library/index.html
          grep -q 'resolver 127.0.0.11' frontend/nginx.conf
          grep -q '8080/api/healthz' update.sh
      - name: Validate launcher JavaScript syntax
        run: |
          node --check frontend/assets/spatial-nav.js
          python3 - <<'PY'
          from pathlib import Path
          import re, subprocess
          pages = [
              Path('frontend/index.html'),
              Path('frontend/catalog/index.html'),
              Path('frontend/library/index.html'),
              Path('frontend/account/index.html'),
              Path('frontend/admin/index.html'),
              Path('frontend/pc/index.html'),
              Path('frontend/achievements/index.html'),
          ]
          for page in pages:
              html = page.read_text(encoding='utf-8')
              blocks = re.findall(r'<script(?:\s[^>]*)?>(.*?)</script>', html, flags=re.S | re.I)
              if not blocks:
                  raise SystemExit(f'no script found in {page}')
              for index, code in enumerate(blocks):
                  if not code.strip():
                      continue
                  target = Path('/tmp') / f'{page.parent.name or "home"}-{index}.js'
                  target.write_text(code, encoding='utf-8')
                  subprocess.run(['node', '--check', str(target)], check=True)
          PY
